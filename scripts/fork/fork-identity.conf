# Declared identity of THIS fork — the guard derives every expected string from
# it (image name, repo links, badges) and cross-checks it against the git remote
# when one is reachable. Declared up front rather than read off the README so
# the source is independent of the thing being measured.
repo=phanlemanh/OneFlow
