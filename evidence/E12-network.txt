# E12 network truth — SSE task/wait payload for the Run that failed on missing MODAL_TOKEN_ID

GET http://localhost:3001/api/task/create → 200 OK
GET http://localhost:3001/api/task/wait?taskId=5VGUmJLgajX4ST4gXM_UG → 200 OK (SSE stream)

Stream body:
data: {"id":"5VGUmJLgajX4ST4gXM_UG","status":"RUNNING","nodeId":"2a0ab3e5-147a-49a3-a692-b89fb9f6e879","data":{"message":"Task started"}}

data: {"id":"5VGUmJLgajX4ST4gXM_UG","status":"FAILED","nodeId":"2a0ab3e5-147a-49a3-a692-b89fb9f6e879","data":{"message":"Task execution failed","error":"Missing required env var MODAL_TOKEN_ID (also missing: MODAL_TOKEN_SECRET)"}}

Scoping: app origin is http://localhost:3001 (config.yaml declares dev_server.url=http://localhost:3000,
but port 3000 was occupied by an unrelated pre-existing process on this machine returning HTTP 500 for
unrelated requests; Next.js auto-selected 3001 and printed "Port 3000 is in use by process 97801, using
available port 3001 instead." All app-scope requests observed above are 200 OK at the HTTP layer; the
FAILED status is the plugin-executor's own pre-flight rejection surfaced as SSE payload content, not a
transport-level or 4xx/5xx failure — no FAIL-eligible request errored.
Other requests during the sequence: two 404s for /plugins/oneflow-local-pyscenedetect.svg and
/plugins/oneflow-local-ffmpeg.svg (icon assets for plugin ids that do not exist under that name — the
real ids are oneflow-api-pyscenedetect / tongflow-modal-ffmpeg) — static asset 404s, note-only per scoping
rules, not a fail condition.

# Step 3 — key entered and saved from the node's inline form

PUT http://localhost:3001/api/settings/env → 200 OK
Response body:
{"env":{"MODAL_TOKEN_ID":"mak-1234567890abcdefFAKEMODALTOKEN"},"verdicts":{"MODAL_TOKEN_ID":{"works":false,"checked":false,"detail":"Chưa kiểm tra được khoá cho MODAL_TOKEN_ID (no prober)."}}}

data/settings.json on disk after save (confirms the write actually persisted server-side, not just an optimistic UI state):
{
  "MODAL_TOKEN_ID": "mak-1234567890abcdefFAKEMODALTOKEN"
}

DOM check throughout the whole sequence (step1 → step2 → step3): document.querySelectorAll('[role="dialog"]').length === 0 and [aria-modal="true"] count === 0 at every step; location.href stayed http://localhost:3001/workspace (never navigated to /settings or opened a settings route). The env value above was written from the node's own inline NodeKeyPrompt form (src/components/workspace/nodes/base/abi-node-shell.tsx useNodeKeyGate -> saveAndVerifyKey -> PUT /api/settings/env), not from the settings dialog.
