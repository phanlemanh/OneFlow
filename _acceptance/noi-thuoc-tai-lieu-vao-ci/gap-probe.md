---
slug: noi-thuoc-tai-lieu-vao-ci
at: 2026-09-01T17:45:00Z
verdict: findings
p0: 1
p1: 3
p2: 1
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | contract + evals | Chế độ `orphans` KHÔNG được lệnh nào chạy trong toàn hồ sơ: E1/E2/E3 chỉ đọc YAML, E4 tự giới hạn ở hai chế độ phá được, E5 khai nó nằm ngoài phép phá. Một trong bốn step vào CI với 0 lượt thực thi. | Implementer viết `--mode orphans` trong khi thước nhận positional. Cả 9 ô vẫn xanh (shape thấy tên script là khớp needle; teeth khai bỏ qua có tên nên đúng AC-5). Merge xong, PR đầu trên main gặp job đỏ vĩnh viễn — hoặc tệ hơn, thước thoát 0 vì không tìm thấy base và không bao giờ canh gì. | Vế xanh cho chuỗi `orphans` rút từ ci.yml, chạy trên KHO THẬT. | **fixed:** AC-4 mở rộng — vế xanh chạy CẢ BỐN chuỗi mới trên kho thật, kể cả `orphans` và bộ răng. Không đụng quyết định từ chối `git init` cho cây thăm dò: kho thật vốn có `.git`. Vế `fetch-depth: 0` thì E1 đã phủ — đo lại `shape` xác nhận nó assert nguyên văn chuỗi đó. |
| P1 | contract + evals | AC-6 không có chiều đỏ riêng; fixture của E7 có thể đỏ vì lý do KHÁC. | Bản vá bỏ `new Map` ở tầng thu thập nhưng giữ `new Set` ở tầng đếm. Ca mới chèn dòng thứ hai → thước đỏ vì `40 ≠ 39`, thông điệp vẫn nêu file + id. Ca PASS, `OK: 8/8`, Cổng 2 ký. Lỗ thật — dòng trùng có id ĐÃ trong manifest, sai org, không đổi số đếm — vẫn bị nuốt lặng. | Đặc tả fixture: id đã có trong manifest, số đếm KHÔNG đổi, chỉ org khác; Then ghim id + file + org sai. | **fixed:** AC-6 và AC-7 viết lại với ba điều kiện fixture bắt buộc + đòi thông điệp nêu BA thứ (id, file, org sai) vì hai thứ đầu cũng có trong ca `readme-missing` nên không phân biệt được hai lỗi. Thêm đối chứng: trùng với org ĐÚNG cũng phải đỏ. |
| P1 | evals | E4/E5 tuyên quét cả LỚP `GUARD_NEEDLES` mà không ma trận toàn phần: không literal nào cho số needle đã kê, số đã phá, số bỏ qua. E5 thay bằng heuristic quan hệ, thoả với vô số cấu hình sai. | Implementer chép mẫu ba guard cũ, viết phép phá cho `readme`, QUÊN `claude`. Đầu ra "kê 7 · phá 4 · bỏ qua 2"; 4 ≠ 7 nên heuristic thoả, E4 exit 0 vì không lệnh nào thất bại. Chuỗi `claude` chưa từng được chứng minh biết đỏ, dù AC-4 hứa đúng điều đó. | Ghim literal `KÊ=7 · PHÁ=5 · BỎ QUA=2`; và chế độ teeth tự thoát khác 0 khi `kê ≠ phá + |bỏ qua|`. | **fixed:** AC-5 và E4/E5 mang ba con số literal, và bất biến đếm thành phép đo (thoát khác 0) chứ không phải lời bình. |
| P1 | contract | AC-2 phát biểu QUAN HỆ ("không gì nuốt mã thoát") nhưng Then chỉ là ba chuỗi có-mặt. | Step viết `run: … synced.sh readme \| tee /tmp/out`. Mã thoát của `tee` thắng; không chuỗi cấm nào xuất hiện → E1/E2 exit 0 và in `OK`. Thước vào CI ở trạng thái không bao giờ đỏ được. | Thay script bằng stub `exit 1` rồi chạy nguyên chuỗi rút được — phải exit khác 0. | **fixed:** ĐO XÁC NHẬN tiền đề — `ci.yml` không khai `shell:` nên `run:` chạy `bash -e` KHÔNG có `pipefail`. AC-2 đổi sang quan hệ; chế độ mới `exit-propagates` + khoá `ntlc_exit_propagates`; ba chuỗi cấm hạ xuống làm quét bổ trợ. |
| P2 | contract + evals | Chế độ `job-count` — mode mà thiết kế §4 nêu đích danh làm lý do không thêm job — không có AC và không ô nào gọi. AC-8 cũng không đòi bằng chứng lane re-pin đã thật sự chạy. | Bốn step mới lỡ cần một `actions/setup-node` riêng; hoặc lane re-pin được làm bằng sửa tay dòng ghim. `check-resign-wave` xanh vì dòng ghim đã trỏ commit mới. Không ô nào chạy lại `job-count`/`check-action-pins` sau khi sửa `ci.yml`. Cổng 2 ký; guard đếm checkout đỏ trên main. | Thêm ô chạy `job-count` + `check-action-pins.sh` trên cây SAU thay đổi; E8 đòi `run_id` của lane re-pin. | **fixed:** thêm **AC-9** + E9 (khoá `ntlc_no_new_checkout` ghép hai lệnh), và `expected` của E8 đòi gói bằng chứng kèm `run_id` của chính lượt lane. |
