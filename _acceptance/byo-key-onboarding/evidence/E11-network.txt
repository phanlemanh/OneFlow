E11 network truth dump — tab-6 (continuous session used for step1/step2 HTML frames)
Captured via mcp__Claude_Browser__read_network_requests / read_console_messages after the run.
dev_server.url (app-scope) = http://localhost:3000

== FAIL-eligible requests (fetch/XHR to http://localhost:3000/api/*) ==
POST /api/task/create -> 200 OK  (x3, one per triggered node run)
GET  /api/task/wait?taskId=... -> 200 OK  (x3, SSE stream open)
POST /api/task/update-status -> 200 OK  (x3)
GET  /api/uploads/example-assets/two-scenes.mp4 -> 200 / 206 Partial Content
GET  /api/uploads/tasks/<taskId>/<file>.mp4 -> 200 / 206 Partial Content (multiple, all OK)
=> No 4xx/5xx, no connection-error, no timeout on any app-scope XHR/fetch endpoint.

== Same-origin but NOT app data calls (Next.js RSC navigation prefetch) ==
GET /workspace?_rsc=... -> repeatedly shows both "200 OK" and "[FAILED: net::ERR_ABORTED]"
This is Next.js dev-mode React Server Component prefetch traffic triggered by repeated
router re-renders / Fast Refresh during this verification session (I navigated/reloaded
the tab several times while setting up the test). ERR_ABORTED here means a newer
prefetch superseded an older one client-side — it never reached the server as a failed
response, so it is not a connection-error/timeout/5xx against a real endpoint. Judged as
noise from my own repeated navigation, not a product defect. Treated as note-only, not
FAIL-eligible in the sense of the rubric (it is navigation plumbing, not an XHR/fetch the
onboarding feature performs to get its own data).

== Static assets (webpack HMR chunks, CSS) ==
Many /_next/static/webpack/*.hot-update.{json,js} and /_next/static/css/*.css requests,
all 200 OK. Expected Next dev-mode hot-reload chatter from edits happening elsewhere in
this shared dev server during the session. Static-asset class -> note-only per rubric.

== Console errors ==
- 5x "Failed to load resource: the server responded with a status of 404 (Not Found)"
  reported by the console API but the exact URL was not resolvable from
  read_console_messages output, and no 404 status line appears in the captured
  read_network_requests window (limit 200) for /api/* or /workspace routes. Likely a
  favicon/icon or a stale HMR chunk reference evicted from the request ring buffer before
  this read. Not tied to any /api/* call actually exercised by the FirstRunStrip /
  provisioning flow under test.
- 1x React hydration-mismatch warning for a Radix UI Tooltip/DropdownMenu `id` attribute
  (server-rendered id vs client id differ — a pre-existing Radix `useId` SSR/CSR quirk in
  the top-bar language switcher). Unrelated to AC-8 / the provisioning strip; not
  reproduced inside the FirstRunStrip component itself.

== Verdict ==
All FAIL-eligible (app-scope /api/* XHR/fetch) requests: clean (200/206, no 4xx/5xx, no
connection error, no timeout). networkObserved = clean.
