---
slug: byo-key-onboarding
at: 2026-08-07T05:15:38Z
verdict: findings
p0: 2
p1: 3
p2: 0
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | contract | AC-10 (test key lúc lưu) không có chỗ đứng trong thiết kế và không eval nào ghim "verify" là gọi thật tới thứ key mở khoá. Bảng Thành phần của design không có file nào làm verification, còn `env-store.server.ts` + `/api/settings/env` nằm trong danh sách "Không đụng". E13/E14 chỉ nói "wrong key phải báo không dùng được" mà không ghim wrong key là **đúng định dạng nhưng bị provider từ chối** | Implementer làm verify bằng regex định dạng key (sk-... đủ độ dài) ngay trong `node-key-prompt.tsx` để khỏi chạm seam bị cấm. E13 xanh (key rác sai định dạng bị báo hỏng), E14 xanh (nhập chuỗi bậy thì đỏ, nhập key thật thì xanh). Ship xong: key đúng định dạng nhưng hết hạn / sai project vẫn được báo "hoạt động", người dùng vẫn phát hiện hỏng ở lần Run — đúng vòng thất bại mà AC-10 sinh ra để chặn | Design nêu tên seam verification (route hoặc module server) và ghi nó vào blast radius để không mâu thuẫn "Không đụng"; E13 ghim hai fixture riêng: key **well-formed nhưng bị từ chối** phải báo hỏng, key rác phải báo hỏng vì lý do khác, và assert verification thực sự phát một lệnh gọi ra ngoài (spy trên client provider), không chỉ trả bool. E14 ghim thông điệp cụ thể chứ không chỉ "trạng thái xác nhận" | fixed: design nêu seam `src/lib/onboarding/key-verify.ts` + đính chính blast radius (env-store/api settings ra khỏi 'không đụng'); AC-10 ghim 'verification means asking the thing the key unlocks, NOT checking the key shape'; E13 đòi spy chứng minh có lệnh gọi ra ngoài + fixture well-formed-nhưng-bị-từ-chối; E14 đòi fixture shape-valid |
| P0 | evals | AC-5 (một nút cài hết) không có eval nào đo QUAN HỆ nút→tập cài. E7 là executor `script` nên không bấm được nút trên strip, chỉ chạm được hàm dưới — đúng dạng lỗ hổng [cache-l4-eviction#F1] (hàm viết đúng, quên call site, eval unit-level vẫn xanh); và không eval nào ghim tập plugin mà `public/example.json` cần là một danh sách literal có **từ 2 phần tử trở lên** — E1 khẳng định phổ quát trên tập có thể rỗng | Ví dụ mới chỉ cần đúng 1 plugin. `installMissing()` viết đúng nên E7 xanh; strip lại nối nút vào `installOne(first)` hoặc mở plugin dialog từng cái — không ai bắt được vì E2 chỉ cần thấy asset hiện ra và tập cần cài có 1 phần tử nên "cài hết" đúng một cách rỗng nghĩa. Trường hợp xấu hơn: parser đổi tên field trả tập rỗng, E1 vẫn xanh (không plugin nào khai required key) và E5 nửa suppression cũng xanh | E1/E5 assert tập plugin của ví dụ bằng danh sách id literal và `length >= 2`; thêm một ui-check cho AC-5 bấm **đúng một lần** nút trên strip rồi assert thư mục plugins chứa đủ mọi id trong danh sách đó, giữ E7 làm nửa backend | fixed: AC-5 viết lại thành quan hệ 'bấm ĐÚNG MỘT LẦN → cả tập'; E1/E5 đòi danh sách id literal + length >= 2 (chặn khẳng định phổ quát trên tập rỗng); thêm E21 ui-check bấm một lần rồi assert mọi id đã cài |
| P1 | contract | AC-2 băng qua backend (task chạy thật, executor sinh asset) nhưng không gắn thẻ `(cross-layer)` và chỉ có bằng chứng tầng UI là E2 — trái chính quy tắc ghi ở đầu `evals.yaml` ("a ui-check alone is never sufficient") | Run thất bại lặng hoặc bị short-circuit, node vẫn hiển thị video mẫu đóng gói sẵn trong `public/example-assets/`. Verifier chụp E2-step4.png thấy "một asset trên canvas" và chấm xanh; thực tế chưa task nào chạy xong, lời hứa cốt lõi "chạm kết quả thật trước khi bị đòi key" chưa hề được chứng minh | Gắn `(cross-layer)` cho AC-2 và thêm một eval `layer: backend-effect` chạy ví dụ dưới nonce riêng, assert có bản ghi task đạt trạng thái succeeded và một file output mới nằm trong asset store, hash khác hash của asset đóng gói; E2 giữ vai trò UI và assert nhãn/thông tin dẫn xuất từ task đó | fixed: AC-2 gắn (cross-layer) + đòi asset phải khác asset đóng gói; thêm E22 `layer: backend-effect` assert task COMPLETED và content hash KHÁC mọi file trong public/example-assets/; E2 ghi rõ frame trùng asset mẫu là FAIL |
| P1 | evals | E18 — eval duy nhất của AC-14 — khai `paths: ["_acceptance/config.yaml"]` và không nêu diff giữa HAI commit nào — lặp lại đúng thiếu sót [stale-scope-by-paths#F2] (định nghĩa diff bằng file nào mà không bằng giữa hai commit nào) | Vòng 2 của implementation thêm một file dưới `sdk/**` (ví dụ bump SDK cho verification của AC-10) nhưng không đụng `config.yaml`; cơ chế staleness coi E18 là không stale nên carry-forward kết quả xanh từ vòng 1. Cổng 2 ký AC-14 "zero file dưới sdk/**" trong khi sdk/** đã đổi. Nếu base không được nêu, phép đo còn có thể tính diff sai cây và luôn rỗng | `paths` của E18 phải là chính tập bị cấm cộng t3 path (`sdk/**`, `src/db/**`, `config/tongflow.abi.json`, `src/generated/abi/**`, `src/lib/plugin-executor/**`) hoặc đánh dấu luôn chạy lại; `expected` ghi rõ base là merge-base của nhánh với `main` và assert cửa sổ diff không rỗng trước khi assert tập cấm rỗng | fixed: paths đổi thành chính tập bị cấm + t3 path (không còn carry-forward xanh khi sdk/** đổi); expected nêu cửa sổ diff merge-base(origin/main,HEAD)..HEAD và đòi assert cửa sổ KHÔNG rỗng trước khi mọi khẳng định vắng mặt có nghĩa |
| P1 | evals | Hai eval của AC-12 đều là khẳng định VẮNG MẶT mà không có positive control và không ghim thông điệp. E1 có nửa suppression (chạy lại trên ví dụ Modal cũ, phải đỏ) còn E16/E17 thì không có gì tương đương | Implementer thêm `navigator.sendBeacon('/api/onboarding/step')` — không phải SDK analytics nên E17 quét tên gói không thấy gì, xanh. E16 đi phễu nhưng dừng sớm vì clone plugin lỗi mạng, network log gần như rỗng nên "không payload hành vi" xanh một cách rỗng nghĩa. AC-12, nửa cấm của cam kết "dữ liệu là của bạn", được ký trong khi beacon đã có trong code | E17 thêm positive control: chạy chính bộ quét trên fixture chứa một beacon/fetch tới endpoint bước phễu và đòi exit khác 0 kèm đường dẫn vi phạm trong output; E16 assert trước rằng log đã bắt được các request hợp lệ (install, settings, task) với số lượng khác 0 và rằng phễu đã đi hết tới bước có asset, rồi mới assert vắng mặt | fixed: E16 đòi positive control (số request hợp lệ khác 0) trước khi assert vắng mặt; E17 mở rộng sang transport shape (sendBeacon, fetch tới origin lạ) + teeth chạy trên fixture có beacon và phải đỏ |
