---
slug: hai-duong-chay-mot-venv
at: 2026-09-08T09:20:16Z
verdict: findings
p0: 2
p1: 2
p2: 1
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | evals | AC-3 là ô sinh ra cả dòng B4 nhưng hai nửa không bị ràng vào cùng một đầu ra. E3b tự dựng cây thư mục "y hệt cái engine tạo ra" bằng tay — fixture viết đúng khuôn bên đọc mong đợi thay vì round-trip lấy từ writer thật. Không ô nào ghim ánh xạ id → tên thư mục con là MỘT giữa hai phía | Bên Python đặt venv tại root/id thô, bên TS chuẩn hoá id nên đặt tại root/id-đã-chuẩn-hoá. E3a xanh vì đo cây do chính engine tạo, E3b xanh vì fixture do tác giả test gõ theo khuôn TS, E4 xanh vì hằng gốc vẫn khớp. Sản phẩm sinh hai thư mục cho cùng một plugin | E3a chạy cấp phát thật rồi xuất manifest ra tệp; E3b NẠP manifest ấy làm fixture. Chiều đỏ: đổi ánh xạ id → thư mục phía Python thì E3b phải đỏ với thông điệp ghim tên thư mục lệch | **fixed:** E3a nay xuất manifest là đầu ra của writer; E3b nạp manifest thay vì gõ tay, và mang chiều đỏ thứ hai cho ánh xạ id |
| P0 | contract + evals | AC-9 có Then hai vế — `prepare_python_env` trả ánh xạ, VÀ `runner` tra ánh xạ ấy theo node — nhưng E10 chỉ đo vế đầu. `runner.py` nằm trong `paths` mà không assertion nào chạm tới | Người hiện thực đổi sang dict đúng hợp đồng nhưng ở runner.py để lại `python=next(iter(pythons.values()))`. E10 xanh vì dict có hai khoá khác nhau. Cả hai plugin vẫn chạy bằng một interpreter — toàn bộ giá trị của bố cục per-plugin mất ở đúng chỗ gọi | Tách một ô đo vế runner: hai node hai plugin_id, thay chỗ sinh tiến trình con bằng bộ ghi lại, khẳng định interpreter từng node bằng đúng `pythons[plugin_id]` và hai giá trị khác nhau | **fixed:** thêm E10b (`layer: backend-effect`) đo đúng vế runner, chiều đỏ ghim hình dạng refactor còn sót |
| P1 | evals | Răng của guard có bốn ca, cả bốn đều là ca ĐỔI giá trị. Không ca nào phủ hướng guard không trích được gì. E4 cũng không ghim SỐ hằng trích được | Refactor gộp hằng đường dẫn vào helper, biểu thức trích không khớp nữa nên trả về rỗng; guard so hai chuỗi rỗng thấy khớp và thoát 0. Bốn ca răng vẫn xanh. Guard tồn tại mà mù — đúng họ hỏng-mà-không-kêu mà gói này khai là kẻ thù | E4 khẳng định guard in ĐÚNG hai giá trị và fail-closed khi một phía trích rỗng. E5 nâng bốn lên sáu ca, hai ca mới xoá hẳn hằng ở mỗi phía; ghim 6/6 và kiểm cả mã thoát lẫn dòng in | **fixed:** E4 nay đòi hai giá trị in ra + fail-closed; E5 lên sáu ca, hai ca cuối là chiều fail-open |
| P1 | contract | CLAUDE.md buộc khai TRẦN VÒNG VERIFY trước khi vòng chạy — T3 tối đa 4 vòng. Không artifact nào khai, dù đây là gói T3 và dù sổ quyết định có nhắc vụ 7 vòng của mo-hoa-b01 | Vòng S4 đầu tìm ra vài điểm ở guard; vì không có trần khai trước, vòng 2–5 tiếp tục vá phép đo của phép đo, và không có mốc nào biến finding còn sót thành nợ có tên. Người duyệt Cổng 1 duyệt một hồ sơ T3 thiếu khai báo bắt buộc mà không biết | Thêm mục vào contract khai trần 4 vòng, kèm câu quy định finding sống sót thành amendment hoặc Known limits. Thẻ Cổng 1 đọc thấy dòng trần thì qua | **fixed:** thêm mục "Trần vòng verify" vào contract, khai trước khi vòng nào chạy |
| P2 | design + contract | Trục C có ba giá trị nhưng chỉ hai có AC. Giá trị thứ ba — kho > PyPI — không có AC, cũng không nằm trong Later hay Never; quyết định noversion không cân trường hợp chạy từ site-packages của một bản dựng chưa publish | Người bảo trì cài SDK từ wheel cục bộ 0.2.24 vào site-packages rồi chạy engine headless. Không có pyproject.toml nên nhánh ghim chạy, pip không giải được, và vì except đã bỏ theo AC-7, cấp phát dừng cứng. Trước gói này nó âm thầm chạy tiếp — một đổi hành vi không ai quyết | Thêm dòng Later/Never nêu đích danh, hoặc thêm AC khai hành vi đã chọn: khẳng định lỗi ném ra mang thông điệp chỉ đường. Chiều đỏ: thông điệp chỉ nêu mã thoát pip thì đỏ | **fixed:** AC-7 mở thêm vế thông điệp phải chỉ ra hai lối; Coverage ghi rõ ô C thứ ba và nói thẳng đây là đổi hành vi |
