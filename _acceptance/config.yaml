# 2-space indentation REQUIRED — the kit's hook parses this file line-by-line.
schema_version: 1
enforcement: strict          # strict | warn | off
recheck: strict              # fresh repo, no legacy reports — start strict (kit guidance).
baseline_minutes: []         # pre-kit acceptance estimates (minutes/feature) — fill after first manual accepts.

# Verify mechanism of this repo (CLAUDE.md checklist): biome lint + tsc + next build
# + vitest + sdk pytest + plugins-scan verify + generated-ABI drift check.
executors:
  test:
    build: "pnpm build"
    typecheck: "pnpm typecheck"
    # Sequential on purpose: next build rewrites .next/types while tsc reads them —
    # parallel suite runs race (lesson: artifact-platform radar-m1 S4-r1).
    build_typecheck: "pnpm build && pnpm typecheck"
    lint: "pnpm lint:check"
    unit: "pnpm test"
    # Run through uv rather than the ambient python3. On macOS the ambient
    # interpreter is Homebrew's, where PEP 668 refuses `pip install pytest`, so
    # `python3 -m pytest` is permanently unavailable — CI passed only because it
    # pip-installs into a runner-owned interpreter first. `--no-project` plus
    # PYTHONPATH keeps sdk/uv.lock and sdk/.venv untouched, so a verify round
    # never dirties the tree; the SDK's runtime deps are named explicitly
    # because PYTHONPATH imports tongflow from source rather than installing it.
    # `--with vietnormalizer==<pin>` added 2026-08-19 (normalize-text-vi Gate 1), pin
    # DERIVED from sdk/pyproject.toml since S4 round 4: a bare `--with vietnormalizer`
    # lets uv resolve whatever PyPI serves as latest, so the golden corpus would certify
    # a reader the SDK does not ship the day 0.2.4 lands. Green today only because latest
    # happens to equal the pin. Same derivation run-normalize-plugin-tests.sh already uses.
    # It becomes a
    # real runtime dependency of the SDK at 0.2.19, and the WHOLE suite — not just this
    # feature's keys — imports tongflow.text.normalize_vi once the module lands. Omitting
    # it here would turn the shared suite red on a collection ImportError that has nothing
    # to do with whatever feature is being verified.
    # The derivation is bound to a name and checked before use. Substituted
    # inline, a pin that cannot be found yields the EMPTY STRING, `--with`
    # swallows the following `python` argument, and every feature using this
    # shared key fails with a uv error pointing nowhere near the cause
    # (S4 round 5 finding). `${pin:?...}` turns that into one legible line.
    sdk_pytest: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q"
    # task-metering Gate 1 (2026-07-25): the test files land during implementation;
    # keys are declared now so eval refs stay portable (artifact-platform pattern).
    unit_metering_schema: "pnpm vitest run src/db/metering-schema.test.ts"
    unit_metering_runner: "pnpm vitest run src/lib/task/metering.test.ts"
    # measure-harness Gate 1 (2026-07-26): files land during implementation.
    unit_measure: "pnpm vitest run src/lib/measure"
    # sdk-distribution-rename Gate 1 (2026-07-26): test file lands during
    # implementation; key declared now so eval refs stay portable.
    sdk_pytest_packaging: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_packaging.py"
    # oneflow-plugin-prefix Gate 1 (2026-07-26): test file lands during implementation.
    unit_plugin_id: "pnpm vitest run src/lib/plugins/plugin-id.test.ts"
    # oneflow-plugin-prefix, added at the T3 amendment: the scanner is the second
    # place the prefix is enforced, and the one that decides registration.
    sdk_pytest_scan_prefix: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_prefix.py"
    # per-plugin-origin Gate 1 (2026-07-27): file lands during implementation.
    unit_official_manifest: "pnpm vitest run src/lib/plugins/official-manifest.test.ts"
    # cache-l1-fingerprint Gate 1 (2026-07-29): both test files land during
    # implementation; keys declared now so eval refs stay portable.
    # One key per criterion, each a distinct pytest node-id on the same two
    # files: sixteen criteria sharing one exit code is the exact
    # pattern the stale-scope-by-paths comment below forbids — a criterion
    # never implemented would look identical to one that passed.
    sdk_pytest_fingerprint_hashseed_stable: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_stable_across_processes_with_different_hashseed"
    sdk_pytest_fingerprint_business_diff: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_business_input_field_diff_changes_key"
    sdk_pytest_fingerprint_per_run_keys_stripped: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_per_run_keys_stripped_do_not_change_key"
    sdk_pytest_fingerprint_asset_bytes_same: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_asset_same_bytes_diff_file_key_same_key"
    sdk_pytest_fingerprint_asset_ref_diff: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_asset_same_file_key_diff_bytes_changes_key"
    sdk_pytest_fingerprint_missing_rev: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_missing_plugin_rev_is_not_cacheable"
    sdk_pytest_fingerprint_dirty_plugin: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_dirty_plugin_is_not_cacheable"
    sdk_pytest_fingerprint_rev_diff: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_plugin_rev_diff_changes_key"
    sdk_pytest_fingerprint_slot_diff: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_slot_diff_changes_key"
    sdk_pytest_fingerprint_plugin_id_diff: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_plugin_id_diff_changes_key"
    sdk_pytest_fingerprint_model_diff: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_model_none_vs_value_changes_key"
    sdk_pytest_fingerprint_sdk_version: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_sdk_version_patch_same_minor_diff"
    sdk_pytest_fingerprint_vectors_exact: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint_vectors.py::test_vectors_match_character_for_character"
    sdk_pytest_fingerprint_vectors_guard: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint_vectors.py::test_vector_guard_catches_schema_version_bump"
    sdk_pytest_fingerprint_digest_form_parity: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_digest_form_matches_normalize_call"
    sdk_pytest_fingerprint_dict_order_stable: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_dict_key_insertion_order_does_not_change_key"
    # cache-l2-store Gate 1 (2026-07-30): every file below lands during
    # implementation. One key per criterion, addressed by pytest node-id —
    # criteria sharing one exit code make a never-implemented case look
    # identical to a passing one (lesson: cache-l1-fingerprint review round 1).
    sdk_pytest_l2_hit_after_miss: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_second_run_hits_cache_with_zero_plugin_calls"
    sdk_pytest_l2_blob_into_run_store: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_hit_reputs_blobs_into_current_run_store"
    sdk_pytest_l2_no_entry_on_failure: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_failed_call_never_writes_an_entry"
    sdk_pytest_l2_unusable_entry_is_miss: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_unusable_entry_is_treated_as_miss"
    sdk_pytest_l2_write_failure_survives: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_unwritable_cache_dir_does_not_break_the_run"
    sdk_pytest_l2_deleted_dir_same_result: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_deleting_cache_dir_keeps_results_identical"
    sdk_pytest_l2_slot_not_allowlisted: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_slot_outside_allowlist_is_never_cached"
    sdk_pytest_l2_tenant_missing: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_missing_tenant_is_not_cacheable"
    sdk_pytest_l2_tenant_isolation: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_two_tenants_sharing_data_dir_do_not_cross_serve"
    sdk_pytest_l2_dirty_plugin: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_dirty_plugin_is_not_cacheable_end_to_end"
    sdk_pytest_l2_batch_partial_hit: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_batch_partial_hit_calls_only_the_misses"
    sdk_pytest_l2_blob_dedupe: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_shared_asset_stores_one_blob"
    sdk_pytest_l2_abi_digest_in_key: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_abi_change_invalidates_the_key"
    sdk_pytest_l2_engine_rejects_empty_tenant: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_engine_rejects_empty_tenant"
    # AC-14 is (cross-layer): this is its TS half. The engine half is the
    # sdk_pytest_l2_engine_rejects_empty_tenant key above.
    # -t scopes each criterion to its own test: two criteria live in this one
    # vitest file, and sharing its exit code is the pattern forbidden above.
    unit_l2_tenant_sentinel: "pnpm vitest run src/lib/task/engine-delegate.test.ts -t 'tenant sentinel'"
    unit_l2_data_dir_stable: "pnpm vitest run src/lib/task/engine-delegate.test.ts -t 'data_dir is stable'"
    sdk_pytest_l2_input_change_partial_rerun: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_changing_one_node_input_reruns_only_it_and_downstream"
    sdk_pytest_l2_bridge_same_data_dir_hits: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py::test_bridge_reuses_cache_across_two_invocations"
    # cache-l3-tier-b Gate 1 (2026-07-30): files land during implementation.
    # One key per criterion via pytest node-ids (standing rule).
    sdk_pytest_l3_mixed_warm_run: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_mixed_tier_workflow_full_hit"
    sdk_pytest_l3_downstream_edit_keeps_gen: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_editing_downstream_a_keeps_tier_b_generation"
    sdk_pytest_l3_workflow_isolation: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_two_workflows_do_not_share_tier_b"
    sdk_pytest_l3_duplicate_node_fresh: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_duplicated_node_generates_fresh"
    sdk_pytest_l3_no_workflow_id_scoped_off: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_missing_workflow_id_disables_only_tier_b"
    sdk_pytest_l3_allowlists_disjoint: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_tier_lists_are_disjoint_and_pinned"
    sdk_pytest_l3_workflow_scope_unconditional: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py::test_workflow_scope_emitted_unconditionally"
    sdk_pytest_l3_git_status_failure_is_dirty: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_git_status_failure_reads_as_dirty"
    sdk_pytest_l3_bridge_workflow_id: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_bridge_forwards_workflow_id"
    sdk_pytest_l3_l1_l2_evals_on_new_tree: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_fingerprint.py tests/test_fingerprint_vectors.py"
    # AC-9 TS half reuses the engineOptionsFor seam; -t scopes to its own test.
    unit_l3_workflow_id_sentinel: "pnpm vitest run src/lib/task/engine-delegate.test.ts -t 'workflow_id'"
    sdk_pytest_l3_tenant_in_tier_b_key: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_two_tenants_do_not_share_tier_b"
    # Subshell on purpose: a bare `cd sdk && ... && pnpm vitest` leaves the shell in
    # sdk/, where vitest finds zero test files and exits 1 (found at L3 Task 5).
    sdk_pytest_l3_l2_full_rerun: "(cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache.py tests/test_node_cache_tier_b.py) && pnpm vitest run src/lib/task/engine-delegate.test.ts"
    l3_conformance_l0_full_rerun: "(cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_engine_batch.py tests/conformance tests/test_plugin_rev.py) && pnpm vitest run src/lib/abi/conformance.test.ts src/lib/task/node-cached.test.ts src/lib/plugins/plugin-rev.test.ts && bash scripts/conformance/check-suite-discriminating.sh && pnpm tsx scripts/plugins/check-rev-joined-path.ts"
    sdk_pytest_l3_batch_duplicate_variants: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_tier_b_batch_duplicates_generate_per_call"
    # cache-l4-eviction Gate 1 (2026-07-31): every file below lands during
    # implementation. One key per criterion via pytest node-ids (standing rule).
    sdk_pytest_l4_lru_recency: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_sweep_evicts_least_recently_used_first"
    sdk_pytest_l4_cap_converges: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_sweep_brings_usage_under_cap"
    sdk_pytest_l4_under_cap_noop: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_sweep_under_cap_deletes_nothing"
    sdk_pytest_l4_shared_blob_survives: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_shared_blob_survives_eviction_of_one_referrer"
    sdk_pytest_l4_orphan_blob_gc: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_orphan_blobs_deleted_including_pre_l4"
    sdk_pytest_l4_legacy_entry: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_legacy_entry_without_meta_sweepable_and_purge_skips"
    sdk_pytest_l4_sweep_failsafe: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_sweep_failure_never_breaks_the_run"
    sdk_pytest_l4_cache_errors_logged: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_swallowed_cache_errors_emit_one_log_line"
    sdk_pytest_l4_purge_scope: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_purge_removes_only_matching_workflow_entries"
    sdk_pytest_l4_purge_idempotent: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_sweep.py::test_purge_twice_safe_and_collects_orphans"
    sdk_pytest_l4_reuse_off: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_runner_wiring.py::test_reuse_off_touches_nothing"
    sdk_pytest_l4_reuse_invalid: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_runner_wiring.py::test_invalid_reuse_value_raises"
    sdk_pytest_l4_counters: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_runner_wiring.py::test_cache_counters_full_partial_miss_and_failure"
    sdk_pytest_l4_node_cached_event: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_runner_wiring.py::test_full_hit_emits_node_cached_partial_does_not"
    sdk_pytest_l4_abi_guard_bidirectional: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_tier_b.py::test_abi_guard_catches_both_directions"
    sdk_pytest_l4_sweep_wired: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_cache_runner_wiring.py::test_run_workflow_auto_sweeps_to_cap_param_env_default"
    # AC-12 is (cross-layer): consumer half below; the engine half is sdk_pytest_l4_node_cached_event.
    unit_l4_node_cached_consumer: "pnpm vitest run src/lib/task/node-cached.test.ts -t 'applies output once'"
    # AC-13 is (cross-layer): TS halves below; the engine half is sdk_pytest_l4_counters.
    unit_l4_delegate_persists: "pnpm vitest run src/lib/task/engine-delegate.test.ts -t 'cache counters'"
    unit_l4_schema_columns: "pnpm vitest run src/db/metering-schema.test.ts -t 'cache counters'"
    # conformance-l0 Gate 1 (2026-07-28): every file below lands during implementation.
    # compose-overlay Gate 1 (2026-08-02): test files land during implementation.
    # One key per criterion via pytest node-ids (standing rule).
    sdk_pytest_overlay_tier_a_hit: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_overlay.py::test_overlay_second_run_full_hit_zero_plugin_calls"
    sdk_pytest_overlay_partial_rerun: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_node_cache_overlay.py::test_changing_text_reruns_only_overlay"
    sdk_pytest_overlay_conformance: "cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/conformance -k compose_overlay"
    unit_overlay_node: "pnpm vitest run src/components/workspace/nodes/transfer/compose-overlay.test.tsx"
    unit_overlay_exporter: "pnpm vitest run src/lib/workflow/compose-overlay-export.test.ts"
    sdk_pytest_batch: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_engine_batch.py
    sdk_pytest_conformance: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/conformance
    sdk_pytest_plugin_rev: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_plugin_rev.py
    unit_conformance: pnpm vitest run src/lib/abi/conformance.test.ts
    unit_plugin_rev: pnpm vitest run src/lib/plugins/plugin-rev.test.ts
    unit_node_cached: pnpm vitest run src/lib/task/node-cached.test.ts
    # local-cpu-plugins Gate 1 (2026-08-06): plugin-python-env.test.ts lands during
    # implementation. One key per criterion, addressed by `-t` — four assertions
    # behind one exit code would make a never-implemented case look like a passing
    # one (standing rule since cache-l1-fingerprint review round 1).
    unit_venv_per_plugin: "pnpm vitest run src/lib/plugins/plugin-python-env.test.ts -t 'own directory'"
    unit_venv_common_parent: "pnpm vitest run src/lib/plugins/plugin-python-env.test.ts -t 'one parent'"
    unit_venv_unsafe_id: "pnpm vitest run src/lib/plugins/plugin-python-env.test.ts -t 'escape'"
    unit_venv_chain_per_id: "pnpm vitest run src/lib/plugins/plugin-python-env.test.ts -t 'per plugin id'"
    # AC-18, added at the Gate-1 amendment of 2026-08-07 (Manh): implementation
    # proved the blanket fallback reachable, so it became a criterion.
    unit_venv_fail_loudly: "pnpm vitest run src/lib/plugins/plugin-python-env.test.ts -t 'declares requirements'"
    unit_venv_lite_fallback_kept: "pnpm vitest run src/lib/plugins/plugin-python-env.test.ts -t 'no requirements'"
    # The pre-2026-08-07 shared venv lived AT the path that is now the root of
    # per-plugin venvs, so its corpse would be enumerated as if it were a plugin.
    # byo-key-onboarding Gate 1 (2026-08-07): every file below lands during
    # implementation. One key per criterion — criteria sharing one exit code make a
    # never-implemented case look identical to a passing one (standing rule).
    unit_first_run_readiness: "pnpm vitest run src/hooks/use-first-run-readiness.test.ts"
    unit_example_requirements: "pnpm vitest run src/lib/onboarding/example-requirements.test.ts"
    unit_failure_actions: "pnpm vitest run src/lib/onboarding/failure-actions.test.ts"
    unit_provisioning_events: "pnpm vitest run src/lib/plugin-executor/provisioning-events.test.ts"
    # BKO_E22 gates the heavyweight end-to-end run (network clones + venv
    # provisioning) — same pattern as the E7/E8 install harness: the eval sets
    # the flag, the plain unit suite sees it skipped.
    unit_example_run_produces_new_asset: "BKO_E22=1 pnpm vitest run src/lib/onboarding/example-run.test.ts"
    # Both halves of AC-10 in one command: the unit file drives verifyKey's
    # own logic, the route file proves the REAL save path calls it (nothing
    # injected but the network itself) — a spy on an injected prober cannot
    # see the road to the function.
    unit_key_verified_on_save: "pnpm vitest run src/lib/onboarding/key-verify.test.ts src/app/api/settings/env/route.test.ts"
    unit_venv_legacy_migration: "pnpm vitest run src/lib/plugins/plugin-python-env.test.ts -t 'legacy'"
    sdk_pytest_scope_with_deploy: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_deploy_with_image_imports_registers_slot
    sdk_pytest_scope_with_scan: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_scan_with_image_imports_registers_and_is_quiet
    sdk_pytest_scope_nested: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_block_matrix_covers_the_ten_keywords tests/test_scan_scope.py::test_non_scope_block_collects_import tests/test_scan_scope.py::test_nested_blocks_collect_import
    sdk_pytest_scope_function_local: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_function_local_import_is_not_collected tests/test_scan_scope.py::test_class_body_import_is_not_collected
    sdk_pytest_scope_import_spellings: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_import_spelling_inside_block
    sdk_pytest_scope_prior_behaviour: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_prior_behaviour_module_level_and_try
    sdk_pytest_reason_not_sdk_model: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_reason_names_defining_file_and_line
    sdk_pytest_reason_missing_annotation: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_reason_missing_annotation
    sdk_pytest_reason_missing_param: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_reason_missing_param
    sdk_pytest_reason_single_source: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_reason_single_source_mutation
    # scan-scope-diagnostics Gate 1 (2026-08-18): every test below lands during
    # implementation; keys declared now so eval refs stay portable. One key per
    # criterion, addressed by pytest node-id — criteria sharing one exit code make a
    # never-implemented case look identical to a passing one (lesson: cache-l1 round 1).
    sdk_pytest_diag_block_reason: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_reason_for_malformed_def_in_module_block
    sdk_pytest_diag_block_matrix: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_rejection_block_matrix_covers_the_ten_keywords tests/test_scan_diagnostics.py::test_rejection_in_non_scope_block tests/test_scan_diagnostics.py::test_rejection_in_nested_blocks
    sdk_pytest_diag_block_wellformed: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_well_formed_def_in_block_registers_and_is_quiet
    sdk_pytest_diag_closure_quiet: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_closure_def_is_not_reported
    sdk_pytest_diag_class_body_quiet: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_class_body_method_is_not_reported_by_dir_scan
    sdk_pytest_diag_shared_walker: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_module_scope_gate_and_import_collector_share_one_walker
    sdk_pytest_diag_syntax_error: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_syntax_error_names_its_own_file_and_line tests/test_scan_diagnostics.py::test_syntax_error_names_the_broken_file_not_entry
    sdk_pytest_diag_unreadable: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_unreadable_file_names_the_read_failure tests/test_scan_diagnostics.py::test_undecodable_file_names_itself_and_spares_other_plugins tests/test_scan_diagnostics.py::test_nul_byte_source_names_itself_and_spares_other_plugins
    sdk_pytest_diag_deploy_error: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_deploy_parse_error_surfaces_in_scan_errors
    sdk_pytest_diag_parse_wording: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_parse_failure_wording_single_source_mutation
    sdk_pytest_diag_inference_merge: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_deploy_and_inference_reasons_both_survive
    sdk_pytest_diag_inference_dedupe: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_ordinary_shape_reports_each_reason_once
    # Added after the S1 gap-probe (P0-1): the BARE module-level def had no criterion —
    # every other case is nested, and check-scan-noise only reddens on GAINED noise, so a
    # lost diagnostic on the commonest shape was invisible to all 17 evals.
    sdk_pytest_diag_bare_module: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_diagnostics.py::test_bare_module_level_malformed_is_still_named tests/test_scan_diagnostics.py::test_bare_module_level_well_formed_stays_quiet
    # gap-probe P0-2: AC-6 promises FOUR collection cases unchanged; the negatives alone
    # are half a floor. All four of the parent's own node-ids, run as one criterion.
    sdk_pytest_diag_import_floor: cd sdk && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions python -m pytest -q tests/test_scan_scope.py::test_block_matrix_covers_the_ten_keywords tests/test_scan_scope.py::test_non_scope_block_collects_import tests/test_scan_scope.py::test_nested_blocks_collect_import tests/test_scan_scope.py::test_prior_behaviour_module_level_and_try tests/test_scan_scope.py::test_function_local_import_is_not_collected tests/test_scan_scope.py::test_class_body_import_is_not_collected
    # add-media-library Gate 1 (2026-08-19): test files land during implementation;
    # keys declared now so eval refs stay portable. One key per seam, not per test
    # file bundle — a seam that is never implemented must not hide behind another
    # seam's exit code (standing rule, lesson: cache-l1 round 1).
    unit_aml_config: "pnpm vitest run src/lib/media-library/config.server.test.ts"
    unit_aml_client: "pnpm vitest run src/lib/media-library/client.server.test.ts"
    unit_aml_import: "pnpm vitest run src/lib/media-library/import.server.test.ts src/lib/media-library/import-roundtrip.server.test.ts"
    unit_aml_ext: "pnpm vitest run src/lib/media-library/extension.test.ts src/lib/media-library/extension-serving.server.test.ts"
    unit_aml_node: "pnpm vitest run src/components/workspace/nodes/add/add-media-library-node.test.tsx"
    unit_aml_wiring: "pnpm vitest run src/lib/workflow/media-library-wiring.test.ts"
    # Sinh sau gap-probe P0: đo call-site qua chính route, không qua lời gọi hàm.
    unit_aml_route: "pnpm vitest run src/app/api/media-library/route.test.ts"
    # normalize-text-vi Gate 1 (2026-08-19): corpus vang song trong kho nay (design §3), nen moi
    # eval doc-thanh-chu la pytest SDK chu khong phai guard clone repo plugin nhu ho overlay_*.
    # --with vietnormalizer==<pin tu pyproject>: engine chuan hoa la dep runtime cua SDK
    # tu 0.2.19; pin derive chu khong de uv tu chon ban moi nhat (S4 round 4 finding).
    sdk_pytest_normalize_money: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py::test_numbers_and_money_golden"
    sdk_pytest_normalize_datetime: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py::test_datetime_golden"
    sdk_pytest_normalize_identifiers: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py::test_identifiers_units_abbrev_golden"
    sdk_pytest_normalize_ambiguous: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py -k 'ambiguous_policy_pinned or ambiguous_d_matrix or declared_uncovered_d_cells'"
    # Tách từ sdk_pytest_normalize_ambiguous (phản biện context sạch F2,
    # 2026-08-27): ba tiêu chí khác nhau dùng chung một lệnh thì ba dòng bằng
    # chứng thực chất là MỘT exit code — hai tiêu chí chưa đo vẫn xanh theo ké.
    sdk_pytest_normalize_typographic_dash: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py -k 'typographic_dashes_read_as_ascii or dash_matrix or compound_word_hyphen'
    sdk_pytest_normalize_range_shapes: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py -k 'number_dash_number_that_is_not_a_range or dotted_thousand_ranges_are_not_refused or ambiguous_policy_pinned'
    sdk_pytest_normalize_error_codes: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py::test_refusal_paths_return_stable_codes
    unit_tts_warning_call_site: pnpm vitest run src/hooks/use-export-warning-toast.test.ts
    sdk_pytest_normalize_residual_fail: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py::test_residual_tokens_fail_and_are_listed"
    sdk_pytest_normalize_residual_pass: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py::test_clean_input_has_no_digits_left"
    sdk_pytest_normalize_idempotent: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py::test_idempotent_and_byte_identical"
    sdk_pytest_normalize_edges: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_normalize_vi.py::test_edge_inputs"
    sdk_pytest_node_cache_normalize: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_node_cache_normalize.py"
    sdk_pytest_tier_a_allowlist: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/test_node_cache.py::test_tier_a_allowlist_pinned_and_in_abi"
    # --- chong-doc-sai-em-ru, S4 vòng 1 ---------------------------------
    # Năm khoá DƯỚI ĐÂY là khoá MỚI, không phải sửa khoá của hợp đồng gốc.
    # Lý do: bằng chứng của `normalize-text-vi` đã ký ghim theo chuỗi lệnh; đổi
    # chuỗi đó làm hồ sơ đã ký nói về một phép đo khác. Nhưng năm eval của hồ sơ
    # này trỏ vào bài kiểm CŨ không chứa ca nào của hành vi mới, nên bằng chứng
    # của chúng là exit code của một phép đo khác (S4 vòng 1). Mỗi khoá dưới đây
    # chạy CẢ bài kiểm cũ (ca âm "vẫn đọc đúng như cũ") LẪN bài kiểm mới.
    sdk_pytest_normalize_dictionary_matrix: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py -k 'identifiers_units_abbrev_golden or dictionary_matrix_covers_every_entry or every_dictionary_entry_expands_and_is_anchored'
    sdk_pytest_normalize_day_word: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py -k 'datetime_golden or day_word_survives_an_unreadable_date'
    sdk_pytest_normalize_slash: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py -k 'residual_tokens_fail_and_are_listed or surviving_slash_is_refused or readable_slash_is_not_flagged'
    sdk_pytest_normalize_brand: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py -k 'idempotent_and_byte_identical or unstable_brand_token_is_refused_by_name or stable_latin_token_still_reads'
    sdk_pytest_normalize_url: cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with "${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}" python -m pytest -q tests/test_normalize_vi.py -k 'edge_inputs or url_is_refused_by_name'
    sdk_pytest_normalize_conformance: "cd sdk && . ../scripts/lib/sdk-version.sh && pin=$(reader_pin) && PYTHONPATH=. uv run --no-project --with pytest --with tomli --with pydantic --with typing_extensions --with \"${pin:?no vietnormalizer pin derived from sdk/pyproject.toml}\" python -m pytest -q tests/conformance/test_conformance.py -k normalize"
    unit_normalize_node: "pnpm vitest run src/components/workspace/nodes/transfer/normalize-text-vi.test.tsx"
    unit_normalize_export: "pnpm vitest run src/lib/workflow/normalize-text-vi-export.test.ts"
    unit_tts_order_violation: "pnpm vitest run src/lib/workflow/tts-order-guard.test.ts -t violation"
    unit_tts_order_compliant: "pnpm vitest run src/lib/workflow/tts-order-guard.test.ts -t compliant"
    unit_tts_family_matches_abi: "pnpm vitest run src/lib/workflow/tts-order-guard.test.ts -t two-way"

    # --- chong-mat-khoa-byo (31/08) ---
    # Mọi khoá dưới đây chạy trên THƯ MỤC TẠM THẬT (test tự đặt TONGFLOW_DATA_DIR)
    # với seam thật qua alias @ext -> src/ext-default của vitest.config.ts. Không
    # khoá nào mock lớp lưu trữ: mock chính là cách hồ sơ trước đo trượt một nửa.
    unit_byo_seam: "pnpm vitest run src/lib/settings/env-store.server.test.ts -t 'seam'"
    unit_byo_read_ok_absent: "pnpm vitest run src/lib/settings/env-store.server.test.ts -t 'ok vs absent'"
    unit_byo_read_unreadable: "pnpm vitest run src/lib/settings/env-store.server.test.ts -t 'unreadable matrix'"
    unit_byo_load_tolerant: "pnpm vitest run src/lib/settings/env-store.server.test.ts -t 'stays tolerant'"
    unit_byo_route_get: "pnpm vitest run src/app/api/settings/env/route.unreadable.test.ts -t 'GET'"
    unit_byo_route_put_refuse: "pnpm vitest run src/app/api/settings/env/route.unreadable.test.ts -t 'refuses and leaves the blob'"
    unit_byo_route_put_force: "pnpm vitest run src/app/api/settings/env/route.unreadable.test.ts -t 'replaceUnreadableStore'"
    unit_byo_route_no_regression: "pnpm vitest run src/app/api/settings/env/route.test.ts"
    # Mount settings-dialog THẬT (RTL + @vitest-environment jsdom, đúng khuôn
    # compose-overlay/normalize-text-vi/add-media-library-node đã dùng) với fetch
    # giả trả 503. Ô này sinh ra từ gap-probe P0: bản đầu để AC-10 dùng lại đúng
    # lệnh của E5, nên không ô nào chạm màn Cài đặt thật.
  script:
    verify_plugins: "pnpm verify:plugins"
