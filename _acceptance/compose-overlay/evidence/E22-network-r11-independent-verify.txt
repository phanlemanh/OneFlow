=== Independent verifier round 11 (wf-label ui:E22) — fresh subagent, doer != grader ===
Repo: /Users/manh-macmini/dev/oneflow, branch feat/compose-overlay, HEAD 17b3eaf (no code
changes to compose-overlay.tsx/compose-overlay-ops-editor.tsx/compose-overlay-op-form.tsx since
the 4447f41 media-kind-resolver fix that r10 already verified).

Scoping: dev_server.url = http://localhost:3000 (config.yaml). No dev_server.api_base declared.
FAIL-eligible set = fetch/XHR to http://localhost:3000; no third-party requests observed; static
assets (image/video/font/.map/favicon) at app-origin = note-only per the scoping rule.

Self-started dev server: `pnpm dev` (background nohup on port 3000; ran gen:abi first per the
project's predev hook), polled http://localhost:3000 until non-000 curl status — ready at first
poll (~2s, log shows "Ready in 1385ms").

Fresh tab (tab-22, created new this round), two consecutive navigations to
http://localhost:3000/workspace. Full request listing reviewed both times:

All requests 200 OK except, on every navigation:
  GET /api/uploads/tasks/e22-fixture/vid-src.png -> 404 Not Found
  GET /api/uploads/tasks/e22-fixture/vid-src.mp4 -> 404 Not Found (x2 per nav)
img-src.png and logo-src.png (same e22-fixture task) resolve 200 OK on every navigation.

These are GET requests to /api/uploads/[...path] (src/app/api/uploads/[...path]/route.ts), which
serves stored media bytes with a long-lived Cache-Control header — a static-asset endpoint (image/
video file bytes), not a business XHR. Classified as static asset per the scoping rule's media
exemption, identical to img-src.png/logo-src.png which sit at the same path shape and only differ
by extension. This is the same pre-existing e22-fixture stub-data gap documented in every prior
round's network evidence (E22-network.txt rounds 2-3 through E22-network-r6..r10-independent-
verify.txt) — unrelated to compose-overlay.tsx or any code path this feature touches.

No 5xx, no connection errors, no timeouts observed anywhere in the FAIL-eligible set.

networkObserved = clean (app-scope traffic observed; every FAIL-eligible request is 200 OK once
static-asset 404s are excluded per the scoping rule).
