E15 network dump (dev_server.url = http://localhost:3000, app-scope requests only)

All document/JS/CSS/font/api requests returned 200 OK.

3 requests marked FAILED by the browser tool:
  GET http://localhost:3000/proto/normalize-text-vi?_rsc=bEKaCk_haC-59Ljq -> 200 OK [FAILED: net::ERR_ABORTED]  (x3)

Analysis: these are Next.js RSC background-prefetch fetches, not server/connection failures.
The underlying HTTP status is 200 OK; net::ERR_ABORTED means the browser cancelled the
in-flight request client-side. They occurred while I was interactively re-navigating the
same tab (setting the NEXT_LOCALE cookie, then navigating idle -> wired) while the Next
dev server was also emitting webpack hot-update events (Fast Refresh) in the background —
a superseded prefetch gets aborted by the newer navigation. This is an artifact of my own
exploratory browsing in the live tab, not of the evidence-capture flow: state-1-idle.png
and state-2-wired.png were produced by the separate hardcoded `node scripts/ui-capture.mjs`
invocations (fresh page load each, no interleaved navigation), whose network activity is
not part of this interactive tab's log.

No connection-error, no timeout, no status >= 500, no unexpected 4xx among app-scope
(localhost:3000) requests. No third-party requests observed.
