E15 network dump — http://localhost:3000/proto/normalize-text-vi (with NEXT_LOCALE=vi cookie)
Scope: dev_server.url = http://localhost:3000 (config.yaml:561)

Console messages: [info] React DevTools hint, [warn] React Flow "parent container needs a width and a height" (layout warning, not an error), [log] task-store handler registration/unregistration.
onlyErrors=true → No console logs (zero error-level entries).

Network requests (all app-origin, all 200 OK):
GET /_next/static/media/22a5144ee8d83bca-s.p.woff2 → 200
GET /_next/static/media/7d4881bb7e1bf84d-s.p.woff2 → 200
GET /_next/static/css/app/layout.css → 200
GET /_next/static/css/app/proto/[slug]/page.css → 200
GET /_next/static/chunks/webpack.js → 200
GET /_next/static/chunks/main-app.js → 200
GET /_next/static/chunks/app-pages-internals.js → 200
GET /_next/static/chunks/app/proto/[slug]/page.js → 200
GET /_next/static/chunks/app/layout.js → 200
GET /api/plugins/registry → 200
GET /proto/normalize-text-vi → 200
(page reloaded 3x during interaction — pattern repeats identically each time, all 200)

networkObserved = clean (app-scope traffic seen, all OK; zero FAIL-eligible failures)
