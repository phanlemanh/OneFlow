E15 network truth dump — verifier round, tree 4b9c1c7a97da1dde817e35ee240111d2cd6d889a
dev_server.url scope = http://localhost:3000

=== Interactive browser session (supplementary console/network check, NOT the evidence captures) ===
First check (tab "seed", after cookie-set + re-navigate over an already-mounted SPA):
  All requests 200 OK EXCEPT:
  GET http://localhost:3000/proto/normalize-text-vi?state=wired&_rsc=6gHWkmYAZ4ZvhWVz -> 200 OK [FAILED: net::ERR_ABORTED]
  (Next.js App Router RSC prefetch request superseded/cancelled by a subsequent client-side
   navigation I triggered manually while the app was already mounted. Not a server error —
   response status was 200; the browser cancelled receiving it because a newer navigation
   started. Self-induced by my re-navigation methodology, not a product defect.)

Control test (fresh isolated tab "tab-1", single navigate, no prior SPA state):
  ALL requests 200 OK, ZERO ERR_ABORTED, ZERO failures. Confirms the ERR_ABORTED above was an
  artifact of my repeated in-app navigation, not something that occurs on a normal/fresh load
  (which is exactly how scripts/ui-capture.mjs loads each state for the pinned evidence PNGs).

=== Console messages (both checks) ===
No [error]-level entries in either session. Only [info] (React DevTools banner), [warn]
([React Flow] parent container width/height — a known dev-mode layout warning, not an error),
and [log] (task-store handler registration/unregistration, Fast Refresh).

=== Verdict ===
No FAIL-eligible failure to app origin on a clean/fresh load. The one ERR_ABORTED seen was
self-induced test noise from an already-mounted SPA re-navigation, not reproducible on fresh load.
