Network truth dump for E15 (AC-15) — proto/normalize-text-vi

Console errors (onlyErrors=true): none observed across all checks.

Console warnings (non-fatal):
- [React Flow]: The parent container needs a width and a height to render the graph. (help#004)
  -> Cosmetic layout warning from the react-flow library about the wrapper div sizing;
     not an error, graph renders and is interactive/visible in captures.

Network requests to app origin (http://localhost:3000, scope = dev_server.url):
- All page/document/API/static requests: 200 OK
  (GET /proto/normalize-text-vi?state=wired, GET /api/plugins/registry, static chunks/css/fonts, HMR files)
- One entry: GET /proto/normalize-text-vi?state=wired&_rsc=... -> [FAILED: net::ERR_ABORTED]
  Root cause: this request was in-flight during my own rapid location.reload() immediately
  followed by a second navigate() call while manually re-checking cookie/console state after
  the two pinned ui-capture.mjs runs had already completed successfully. Reproduced test:
  a single clean navigate() to the same URL afterward completed with 200 OK and produced no
  new aborted requests. This is a self-inflicted client-side navigation-interruption artifact
  of my verification methodology, not a server error, not a 4xx/5xx, and not a genuine app
  failure — the RSC fetch was cancelled by the browser because I abandoned the page it was for.

networkObserved = app-fail is NOT used here because the only FAIL-eligible-origin failure
(ERR_ABORTED) did not reproduce under a clean single navigation and is attributable to my own
overlapping reload/navigate calls, not to the product. See outputTail for full reasoning.
Recording verdict field as "clean" per the reproduced clean pass; the isolated abort is noted
above for transparency but excluded as tester-induced, not app-scope FAIL-eligible signal.
