---
slug: skill-system-v1
at: 2026-09-16T12:00:48Z
verdict: findings
p0: 1
p1: 4
p2: 0
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | design | AC-10 hứa hai trạng thái mà API không có dữ liệu để hiện: tiến trình theo từng bước (ST-chay-dang-chay) và bước hỏng (ST-ketqua-loi). Thiết kế §2.4 chỉ khai runs trả status, skillId, outputs, error, không có trường bước; SSE cũng không có trường nào được ghim. AC-10 mang nhãn cross-layer mà không có eval backend-effect. E10 dựng fetch và SSE giả bằng tay đúng khuôn bên đọc, không round-trip từ route thật. E7 chỉ đòi phong bì lỗi, không ghim hình dạng — đúng khuôn [hai-duong-chay-mot-venv#F1]. | Route thật trả error dạng code và message, không có mảng bước. Component đọc steps và failedStep như fixture tay. E10 xanh, E7 xanh; E13 chỉ đi đường suôn sẻ nên không chạm màn lỗi. Người dùng thấy màn tiến trình trống và câu lỗi không có bước hỏng. Cổng 1 duyệt một trạng thái UI chưa có hợp đồng dữ liệu phía máy chủ. | (1) Thiết kế và AC-7 khai nguồn của bước. (2) E7 assert hình dạng cho running, completed, failed. (3) E10 nạp fixture từ đầu ra route thật, không gõ tay. (4) Thêm eval backend-effect cho AC-10: engine giả hỏng node thứ hai, runs trả đúng nodeId đó. | fixed: thiết kế §2.4 khai hai nguồn có sẵn — sự kiện SSE NODE_STARTED, NODE_COMPLETED, NODE_FAILED (mang nodeId) cho tiến trình sống, và steps của GET runs tính từ tasks.error.failures[].nodeId mà engine-delegate đã ghi; AC-7 và AC-10 viết lại theo đó; E7 assert steps cho ba trạng thái, error ghi bằng writer thật của engine-delegate; E10 nạp sự kiện qua đường xử lý sự kiện thật và payload qua handler route thật; thêm E7b backend-effect (dispatchTask thật, NDJSON node_failed n2, runs trả n2 failed) |
| P1 | contract | AC-2 kể năm lớp đầu vào sai nhưng E2 chỉ có bốn ca. Hai skill thật không có tham số enum. Schema params ở thiết kế §2.1 không có min/max. Đối chứng dương của E2 viết dạng phủ định nên xanh cả khi route trả PLUGIN_NOT_INSTALLED hay 500. | Người hiện thực không có chỗ khai khoảng cho do-nhay. Ca ngoài khoảng được thoả bằng số âm bị bắt vì sai kiểu. Nhánh enum chưa có code và chưa ai đo. Đối chứng xanh ngay cả khi route trả PLUGIN_NOT_INSTALLED cho mọi yêu cầu hợp lệ. | Thêm min/max vào §2.1; E2 ma trận năm lớp, ca enum qua manifest fixture; đối chứng khẳng định 200 và tasks tăng đúng 1. | fixed: §2.1 thêm min/max (number) và options (enum); AC-2 ghim tập mã lý do đóng xuất từ params.ts và vế đối chứng dương; E2 viết ma trận năm lớp bằng đúng độ dài SKILL_PARAM_REASONS, ca enum qua manifest fixture đi qua params.ts thật, đối chứng là 200 cộng tasks tăng đúng 1 |
| P1 | contract | AC-9 hứa nhãn Cần cài plugin, dữ liệu đi qua GET /api/skills, nhưng AC-9 không mang nhãn cross-layer và không eval nào gọi route danh sách thật. E9 dùng mock, E12 chạy trên lượt đủ hai plugin. Không dòng Tiền đề nào dựng trạng thái thiếu một plugin. | Route luôn trả installed hoặc đọc thư mục plugin mặc định thay TONGFLOW_PLUGINS_DIR. E9 xanh vì mock, E12 xanh vì đủ plugin. Người dùng thiếu pyscenedetect bấm được Cắt cảnh video rồi mới gặp 400 lúc chạy. | Gắn cross-layer cho AC-9; E9b backend-effect với bộ quét plugin thật, một plugin cài và một thiếu; một ui-check trên lượt chỉ cài ffmpeg. | fixed: AC-9 mang (cross-layer) và nói rõ nguồn là registry plugin thật qua GET /api/skills; route trả missingSlots, giao diện hiện tên bước thay tên plugin; thêm E9b (bộ quét thật trên thư mục plugin fixture, có đối chứng đủ plugin); E9 lấy payload từ handler route thật; thêm tiền đề TD-4b và ui-check E12b trên lượt chỉ cài ffmpeg |
| P1 | evals | AC-13 hứa lỗi nói bằng lời sản phẩm, nhưng ST-bieumau-loi-tham-so hiện câu lý do máy chủ trả và AC-4 trả tên slot — chuỗi máy chủ nằm ngoài Skills.* của i18n. E17b chỉ quét vi.json. E17 chỉ nhận hai ảnh đường suôn sẻ. | params.ts trả reason dạng tiếng Anh kỹ thuật và UI in thẳng; nhãn thiếu plugin in tên slot. E17b và E17 cùng xanh vì không thấy màn lỗi. | Ghim reason là mã tập đóng; E17b đọc tập mã và đòi khoá i18n tương ứng; render lỗi thật từ route; E17 thêm ảnh các trạng thái lỗi. | fixed: thiết kế §2.4 và AC-2 ghim máy chủ chỉ trả mã thuộc SKILL_PARAM_REASONS, giao diện dịch qua Skills.invalid.<mã>; nhãn thiếu plugin dùng tên bước; E17b thêm vế số khoá invalid bằng số mã và vế mỗi slot có khoá tên bước, thêm node vào danh sách từ cấm; E17 nhận thêm bốn khung lỗi, bận, thiếu plugin của design-pass và khung E12b |
| P1 | evals | AC-12 là lời hứa quan hệ nhưng E16 chỉ đối chiếu đường dẫn của một commit tìm bằng diff-filter=A. Không gì chặn commit chuẩn bị ngay trước đó đưa nhánh riêng cho skill thứ hai vào runner, route hay instantiate. Chưa artifact nào nói bộ tham số mẫu sống ở đâu. | Commit N-1 thêm nhánh theo id tach-tieng-video vào runner; commit N sạch; E16 xanh và bằng chứng nền tảng rỗng. Hoặc tham số mẫu nằm trong tệp test làm E16 đỏ oan và người hiện thực nới danh sách cho phép. | E16 thêm vế git grep chuỗi riêng của skill hai ngoài thư mục của nó bằng 0; chạy E3 và E6 ở commit cha; ghim sample-params.json trong thư mục skill; răng cho nhánh id ở commit trước. | fixed: AC-12 viết lại ba vế; thiết kế ghim sample-params.json cạnh mỗi skill; E16 chạy ba vế có tên (đường dẫn · khung đã xanh tại commit cha qua worktree tạm · git grep chuỗi riêng bằng 0 tại HEAD) và răng thứ ba cho nhánh theo id skill ở commit cha |
