---
slug: repin-khong-chay-lai-eval
at: 2026-09-02T11:40:00Z
verdict: findings
p0: 2
p1: 3
p2: 0
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | design | KHÔNG vật nào trong phạm vi GHI `prev_sha`. Thiết kế coi nó là "dữ liệu còn thiếu, tuỳ chọn" và cố ý không sửa nghi thức trong kit — mà nghi thức chính là chỗ sinh dòng repin. Sổ cân "dòng cũ có hợp lệ không", chưa bao giờ hỏi "ai viết dòng mới". | Merge xong, mọi lần ghim sau vẫn theo SKILL của kit và in đúng năm khoá cũ. Ông bà 33→34→35, dòng tính được đứng 0, eval bị nuốt đứng 0 vĩnh viễn. Hàng rào xanh mọi PR và không bao giờ nổ. Câu "con số ấy co lại theo thời gian" sai từ ngày merge, và người duyệt ký vì tin nó đúng. | Một vật do KHO sở hữu sinh `prev_sha`, cộng hàng rào delta đòi dòng MỚI phải có. | **fixed:** mở phạm vi — **AC-1** (chế độ `write` của kho, ghi `prev_sha` lấy từ `verified_commit`, từ chối khi thiếu) và **AC-2** (dòng repin mới so `origin/main` phải có `prev_sha`; dòng cũ đi đường ông bà). Đây không phải sửa kit: nó là cách gõ dòng repin đúng hơn cách gõ tay hiện nay. |
| P0 | evals | E3/E4/E6 dùng chung một lệnh và cả ba xanh với một script TRƠ. Bốn số định ghim (32·33·33·0) đúng bằng đầu ra của một script chỉ đếm dòng rồi xếp tất cả vào nhánh ông bà. Nhánh chính — diff `prev_sha..sha` → giao glob → tra run-log — không chạy trên dữ liệu thật lần nào. | Implementer viết bộ đọc nhưng tra run-log SAI CHỖ (chưa artifact nào định nghĩa bản ghi chạy lại nằm ở file nào, khoá nào). Fixture của bộ răng dùng đúng chỗ implementer chọn nên các ca xanh. Kho thật 0 dòng tính được nên chưa từng tra. Ký. Ngày kit bắt đầu ghi `prev_sha`, hàng rào đỏ hàng loạt hoặc xanh mù. | Bắt in **số dòng tính được** như số độc lập và đòi **> 0** trên kho thật; viết SHAPE + đường dẫn của "bản ghi chạy lại" vào contract trước khi code. | **fixed:** AC-6 nâng lên **năm** số với điều kiện `dòng tính được > 0` — điều chỉ đạt được sau khi `write` ghi một dòng thật, tức nó buộc AC-1 phải HOẠT ĐỘNG chứ không chỉ tồn tại. AC-5 khai thẳng hình dạng bản ghi chạy lại: dòng `{"kind":"eval","run_id":…,"eval":…}` trong chính `run-log.jsonl` của hồ sơ. |
| P1 | evals | E1 có chiều đỏ TỰ MÂU THUẪN (`prev_sha` sai kiểu làm bên đọc từ chối — nhưng tiền đề của cả feature là trường lạ BỊ BỎ QUA), không đối chứng dương, và không đo câu AC-1 thật sự hứa (quan hệ trước/sau trên cây thật). | Implementer chạy, ca "sai kiểu" không đỏ, bèn bỏ chiều đỏ và giữ vế xanh. Vế xanh là hằng-đúng: không nội dung `prev_sha` nào làm bên đọc đỏ được, nên ô chỉ còn chứng minh "JSON hợp lệ vẫn parse được". | Đổi chiều đỏ sang thứ thiết kế TIÊN ĐOÁN đỏ (bỏ khoá `sha`); đo AC đúng chữ bằng so đầu ra hai lượt **từng byte**. | **fixed:** thành **AC-7** — chạy `pre-merge-check.sh` hai lượt trên cây thật (có/không `prev_sha`), đòi đầu ra giống nhau từng byte, và đối chứng dương là **gỡ khoá `sha`** phải làm cả hai bên đọc đỏ. |
| P1 | contract | Luật khớp `paths` không được định nghĩa, và không ô nào đo nó — dù đó là phép tính lõi. Chính `evals.yaml` của hồ sơ này khai `paths: ["_acceptance"]`, thư mục trần. | Implementer dùng minimatch thuần: `_acceptance` chỉ khớp đúng chuỗi ấy. Mọi mục kiểu thư mục trong 357 eval đang khai `paths` thành KHÔNG BAO GIỜ bị chạm. `check` báo 0 eval bị nuốt vĩnh viễn — đúng con số ô đo chờ đợi — nên tất cả xanh, và hàng rào under-report ở đúng lớp hồ sơ đông nhất. | Viết luật vào contract, rồi ca răng đối xứng cho từng dạng, mỗi dạng có vế chạm và vế không-chạm. | **fixed:** **AC-3** định nghĩa luật (bằng đúng mục · nằm trong cây con · khớp glob) và đòi **ma trận toàn phần bốn hình dạng × hai chiều = tám dòng**, mỗi dòng nêu mục nào khớp file nào. |
| P1 | evals | E2 (`plan`) không có ma trận toàn phần và không có chiều đỏ: chỉ đòi ba con số, không nêu hồ sơ nào, cặp sha nào, tập id kỳ vọng là gì. | Tool in `tổng 7 · khai paths 6 · bị chạm 6`; không ai biết 6 có đúng không. Một bộ giao trả về CẢ TẬP (khớp quá rộng) cũng in đúng ba số đó và vẫn thoả luật "không khai paths thì luôn có mặt". Ô xanh mãi. | Ghim fixture có tên + cặp sha + **tập id kỳ vọng đầy đủ**, so bằng phép so tập; thêm chiều đỏ một-file-một-id. | **fixed:** **AC-4** đòi in HAI TẬP (bị chạm và không bị chạm), so bằng phép so tập, cộng ca "thêm đúng một file phải làm đúng một id chuyển tập". |
